import React from 'react';
import PageShell from '../components/PageShell';

const Dashboard = () => {
  return (
    <PageShell
      title="Dashboard"
      subtitle="Overview of the identity management ecosystem."
      hint="Dashboard widgets will appear here once data sources are connected."
    />
  );
};

export default Dashboard;
